/**
 * Visibility inference — re-exported from foundation layer.
 *
 * The canonical implementation lives in `src/domain/foundation/visibility.ts`
 * (zero external dependencies). This file re-exports it for backward compatibility.
 */

export { inferVisibility } from '../foundation/visibility';
