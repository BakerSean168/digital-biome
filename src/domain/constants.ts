/**
 * Shared constants — re-exported from foundation layer.
 *
 * All constants and primitive type aliases now live in
 * `src/domain/foundation/constants.ts` (zero external dependencies).
 * This file re-exports them for backward compatibility.
 */

export * from './foundation/constants';
