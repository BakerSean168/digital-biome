---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="404 - 页面未找到">
  <main class="error-page">
    <div class="error-content">
      <h1 class="error-code">404</h1>
      <h2 class="error-title">页面未找到</h2>
      <p class="error-message">抱歉，您访问的页面不存在或已被移动。</p>
      
      <div class="error-actions">
        <a href="/" class="action-btn primary">返回首页</a>
        <a href="/notes" class="action-btn">浏览笔记</a>
        <a href="/about" class="action-btn">个人主页</a>
      </div>
    </div>
  </main>
</BaseLayout>

<style>
  .error-page {
    min-height: 80vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 2rem;
  }

  .error-content {
    text-align: center;
    max-width: 500px;
  }

  .error-code {
    font-size: 8rem;
    font-weight: 700;
    margin: 0;
    color: var(--accent-color, #0066cc);
    line-height: 1;
  }

  .error-title {
    font-size: 1.5rem;
    margin: 1rem 0;
    color: var(--text-primary, #333);
  }

  .error-message {
    font-size: 1rem;
    color: var(--text-secondary, #666);
    margin-bottom: 2rem;
  }

  .error-actions {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 1rem;
  }

  .action-btn {
    display: inline-block;
    padding: 0.75rem 1.5rem;
    font-size: 0.95rem;
    text-decoration: none;
    border-radius: 8px;
    transition: all 0.2s;
    background: var(--card-bg, #f5f5f5);
    color: var(--text-primary, #333);
    border: 1px solid var(--border-color, #e5e5e5);
  }

  .action-btn:hover {
    border-color: var(--accent-color, #0066cc);
  }

  .action-btn.primary {
    background: var(--accent-color, #0066cc);
    color: #fff;
    border-color: var(--accent-color, #0066cc);
  }

  .action-btn.primary:hover {
    opacity: 0.9;
  }

  :global(html.dark) .action-btn {
    --card-bg: #1e1e3f;
    --border-color: #2d2d4a;
  }

  @media (max-width: 480px) {
    .error-code {
      font-size: 5rem;
    }

    .error-title {
      font-size: 1.25rem;
    }
  }
</style>
